# Transition 动画

### 介绍

使元素从一种样式逐渐变化为另一种样式的效果。

## 代码演示

### 引入

```jsx
import { Transition } from 'czh-weapp'
```

### 基础用法

将元素包裹在 Transition 组件内，在元素展示/隐藏时，会有相应的过渡动画。

```jsx
<Transition 
  show={show}
/>
```

### 动画类型

Transition 组件内置了多种动画，可以通过name字段指定动画类型。

```jsx
<Transition 
  name="fade"
  show={show} 
/>
```

### 高级用法

可以通过外部样式自定义过渡效果，还可以定制进入和移出的持续时间。

```jsx
import style from './transition.module.scss'

<Transition 
  show={show} 
  duration={{
    enter: 300,
    leave: 1000
  }}
  className={style.block}
  enterClass={style.enterClass}
  enterActiveClass={style.enterActiveClass}
  leaveActiveClass={style.leaveActiveClass}
  leaveToClass={style.leaveToClass}
/>
```

```scss title="transition.module.scss"
.block {
  position: fixed;
  top: 50%;
  left: 50%;
  width: 200px;
  height: 200px;
  margin-top: -100px;
  margin-left: -100px;
  background-color: #1989fa;
  border-radius: 10px;
}

.enterActiveClass,
.leaveActiveClass {
  transition-property: background-color, transform;
}

.enterClass,
.leaveToClass {
  background-color: red;
  transform: rotate(-360deg) translate3d(-100%, -100%, 0);
}
```

## API

### Props

<div class="table-container">
| 参数 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| show | 是否展示组件	 | _boolean_ | `true` |
| duration | 动画时长，单位为毫秒	 | _number_ | `300` |
| name | 动画类型, 可选值为fade \| fade-up \| fade-down \| fade-left \| fade-right \| slide-up \| slide-down \| slide-left \| slide-right | _string_ | `fade` |
| lockScroll | 是否阻止滑动穿透 | _boolean_ | `false` |
| enterClass | 定义进入过渡的开始状态。在元素被插入之前生效，在元素被插入之后的下一帧移除。	 | _string_ | - |
| enterActiveClass | 定义进入过渡生效时的状态。在整个进入过渡的阶段中应用，在元素被插入之前生效，在过渡/动画完成之后移除。这个类可以被用来定义进入过渡的过程时间，延迟和曲线函数。	 | _string_ | - |
| enterToClass | 定义进入过渡的结束状态。在元素被插入之后下一帧生效 (与此同时 enter-class 被移除)，在过渡/动画完成之后移除  | _string_ | - |
| leaveClass | 定义离开过渡的开始状态。在离开过渡被触发时立刻生效，下一帧被移除。	 | _string_ | - |
| leaveActiveClass | 定义离开过渡生效时的状态。在整个离开过渡的阶段中应用，在离开过渡被触发时立刻生效，在过渡/动画完成之后移除。这个类可以被用来定义离开过渡的过程时间，延迟和曲线函数。	 | _string_ | - |
| leaveToClass | 定义离开过渡的结束状态。在离开过渡被触发之后下一帧生效 (与此同时 leave-class 被删除)，在过渡/动画完成之后移除。	 | _string_ | - |
</div>

### 动画类型

| 名称 | 说明 | 
| --- | --- |
| fade | 淡入	 |
| fade-up | 上滑淡入	 |
| fade-down | 下滑淡入 |
| fade-left | 左滑淡入 |
| fade-right | 右滑淡入 |
| slide-up | 上滑进入 |
| slide-down | 下滑进入 |
| slide-left | 左滑进入 |
| slide-right | 右滑进入 |


### Events

| 事件名 | 说明 | 参数  |
| --- | --- | --- |
| onBeforeEnter | 进入前触发 | - |
| onEnter | 进入中触发 | - |
| onAfterEnter | 进入后触发	| - |
| onBeforeLeave | 离开前触发 | - |
| onLeave | 离开中触发 | - |
| onAfterLeave | 离开后触发 | - |
| onClick | 点击后触发 | - |

### 样式变量

<div class="table-container">
| 名称 | 说明 | 默认值 | 
| --- | --- | --- |
| --czh-fade-up-enter-leave-y | 上滑淡入： translate3d函数Y轴上的平移量 | `100%` |
| --czh-fade-down-enter-leave-y | 下滑淡入： translate3d函数Y轴上的平移量 | `-100%` |
| --czh-fade-left-enter-leave-x | 左滑淡入： translate3d函数X轴上的平移量 | `-100%` |
| --czh-fade-right-enter-leave-x | 右滑淡入： translate3d函数X轴上的平移量 | `100%` |
| --czh-slide-up-enter-leave-y | 上滑进入： translate3d函数Y轴上的平移量 | `100%` |
| --czh-slide-down-enter-leave-y | 下滑进入： translate3d函数Y轴上的平移量 | `-100%` |
| --czh-slide-left-enter-leave-x | 左滑进： translate3d函数X轴上的平移量 | `-100%` |
| --czh-slide-right-enter-leave-x | 左滑进： translate3d函数X轴上的平移量 | `100%` |
</div>